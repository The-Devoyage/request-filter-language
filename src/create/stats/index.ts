import { HistoricStats, HistoricStatsId, Stats } from "src/schemas";

export const stats = (statsInput: Stats) => {
  return statsInput;
};

export const historicStats = (historicStatsInput: HistoricStats) => {
  return historicStatsInput;
};

export const historicStatsId = (historicStatsIdInput: HistoricStatsId) => {
  return historicStatsIdInput;
};
